import { DefaultTheme } from 'styled-components';

const darkTheme: DefaultTheme = {
  global: {
    bgColor: '#303030',
    textColor: '#f7f7f7',
    mainColor: '#ffffff',
  },
};

export default darkTheme;
